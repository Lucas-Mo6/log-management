server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名
    index index.html;
    root /www/wwwroot/log-management/public;
    
    # 访问日志
    access_log /www/wwwlogs/log-management.log;
    error_log /www/wwwlogs/log-management.error.log;
    
    # 前端静态文件
    location / {
        try_files $uri $uri/ /index.html;
        expires 7d;
        add_header Cache-Control "public, no-transform";
    }
    
    # API代理到后端Node.js服务
    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, no-transform";
    }
    
    # 安全设置
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
}

# HTTPS配置 (可选)
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;  # 替换为你的域名
#     index index.html;
#     root /www/wwwroot/log-management/public;
#     
#     # SSL证书配置
#     ssl_certificate /path/to/your/cert.pem;
#     ssl_certificate_key /path/to/your/key.pem;
#     ssl_session_timeout 5m;
#     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     
#     # 访问日志
#     access_log /www/wwwlogs/log-management-ssl.log;
#     error_log /www/wwwlogs/log-management-ssl.error.log;
#     
#     # 前端静态文件
#     location / {
#         try_files $uri $uri/ /index.html;
#         expires 7d;
#         add_header Cache-Control "public, no-transform";
#     }
#     
#     # API代理到后端Node.js服务
#     location /api/ {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         
#         # 超时设置
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
#     
#     # 静态资源缓存
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
#         expires 1y;
#         add_header Cache-Control "public, no-transform";
#     }
#     
#     # 安全设置
#     location ~ /\. {
#         deny all;
#         access_log off;
#         log_not_found off;
#     }
# }

# HTTP到HTTPS重定向 (可选)
# server {
#     listen 80;
#     server_name your-domain.com;  # 替换为你的域名
#     return 301 https://$server_name$request_uri;
# }